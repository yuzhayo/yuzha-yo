ROLE: You are a precise code migration agent. Do exactly what is listed.
Do not add files, packages, or features not explicitly listed here.
Work inside the repo root and manga/ only.

You are on Phase 6. Phases 1–5 are complete and correct. Do not touch any
Phase 1–5 files unless explicitly listed below.

═══ TASK 1 of 3: Run npm install ═══

From the repo root (not inside manga/), run:

  npm install

Show the last 5 lines of output. If it errors, show the full error.
Do not run npm install inside manga/ directly.

═══ TASK 2 of 3: Update root package.json — replace manga scripts ═══

Open the ROOT package.json (not manga/package.json).

Find and REPLACE any existing manga-related scripts. The exact keys to add or
update (replace the values if the key already exists, add if missing):

  "dev:manga":       "npm run dev --workspace manga",
  "build:manga":     "npm run build --workspace manga",
  "dist:manga":      "npm run dist --workspace manga",
  "dist:manga:win":  "npm run dist:win --workspace manga",
  "dist:manga:mac":  "npm run dist:mac --workspace manga",
  "dist:manga:linux":"npm run dist:linux --workspace manga",
  "typecheck:manga": "npm run typecheck --workspace manga",
  "preview:manga":   "npm run preview --workspace manga"

Do not remove any other existing scripts. Do not change any other part of
root package.json.

═══ TASK 3 of 3: Remove Manga Reader button from yuzha/src/MainScreenUtils.tsx ═══

Open yuzha/src/MainScreenUtils.tsx.

Find the button/entry that:
- Opens port 3003 in a new tab   OR
- Has text "Manga Reader" with an arrow (↗ or similar)   OR
- Calls window.open with port 3003

Remove that button/entry entirely (the full JSX block for it, including any
wrapping <div> or <li> that is ONLY for the manga button).

Do not change anything else in that file.
If you cannot find any manga button, output: "NO MANGA BUTTON FOUND" and skip.

═══ BUG PREVENTION NOTES ═══

BUG-14: The manga button in yuzha calls window.open(getStandaloneUrl(3003)).
Manga is now an Electron app, not a browser tab. This button must be removed
so users are not confused.

BUG-12: electron-vite dev won't open a window in Replit — that is expected.
The verification step below only checks that the typecheck passes and the
scripts are wired. Do not attempt to run electron-vite dev.

Do NOT run electron-vite dev. Do NOT run any Electron app.
Do NOT add any new packages to manga/package.json or root package.json.

═══ VERIFICATION — Run these shell commands and show the output ═══

1.  cat package.json | grep -A3 '"dev:manga"'
2.  cat package.json | grep -A1 '"dist:manga"'
3.  cat package.json | grep -A1 '"typecheck:manga"'
4.  grep -n "3003\|Manga Reader\|manga" yuzha/src/MainScreenUtils.tsx || echo "CLEAN"
5.  cd manga && npx tsc -p tsconfig.node.json --noEmit 2>&1 | head -30
6.  cd manga && npx tsc -p tsconfig.web.json --noEmit 2>&1 | head -30
7.  ls manga/electron/main/
8.  ls manga/src/reader/
9.  ls manga/src/downloader/
10. grep -r "@shared\|mangaDexApi\|MangaDex" manga/src/ || echo "CLEAN"

Stop here. Report the output of all 10 commands.
The migration is complete when commands 5 and 6 both show zero TypeScript errors.