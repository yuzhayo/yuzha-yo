ROLE: You are a precise code migration agent. Do exactly what is listed.
Do not add files, packages, or features not explicitly listed here.
Do not run npm install. Do not start any dev server.
Work inside manga/ only.

You are on Phase 4. Phases 1–3 are complete and correct.
Do NOT touch any Phase 1–3 files (package.json, tsconfig files,
electron.vite.config.ts, electron/**, src/types.ts, src/env.d.ts).

═══ OVERVIEW ═══

Move all reader files from src/ into src/reader/.
Delete 4 MangaDex files entirely.
Modify 4 files during the move.
The result: src/ root has only types.ts, env.d.ts, index.html,
index.css, main.tsx (untouched until Phase 5).

═══ TASK 1 of 9: Create src/reader/ and move unchanged files ═══

Copy these files EXACTLY as-is (no content changes) from src/ to src/reader/:

  src/MangaControls.tsx     → src/reader/MangaControls.tsx
  src/MangaLibrary.tsx      → src/reader/MangaLibrary.tsx
  src/MangaReader.tsx       → src/reader/MangaReader.tsx
  src/MangaToolbar.tsx      → src/reader/MangaToolbar.tsx
  src/MangaUploader.tsx     → src/reader/MangaUploader.tsx
  src/useKeyboardNav.ts     → src/reader/useKeyboardNav.ts
  src/useReaderState.ts     → src/reader/useReaderState.ts
  src/useReadingHistory.ts  → src/reader/useReadingHistory.ts

After copying, in each of these 8 files find any import that says:
  from "../types"   → change to  from "../types"    (already correct — no change needed)
  from "./types"    → change to  from "../types"
  from "../useCbzLoader"    → change to  from "./useCbzLoader"
  from "../useReaderState"  → change to  from "./useReaderState"
  from "../useReadingHistory" → change to  from "./useReadingHistory"
  from "../useKeyboardNav"   → change to  from "./useKeyboardNav"
  from "../MangaReader"     → change to  from "./MangaReader"
  from "../MangaControls"   → change to  from "./MangaControls"
  from "../MangaLibrary"    → change to  from "./MangaLibrary"
  from "../MangaToolbar"    → change to  from "./MangaToolbar"
  from "../MangaUploader"   → change to  from "./MangaUploader"

═══ TASK 2 of 9: Delete 4 MangaDex files from src/ ═══

Delete these 4 files. They must not exist anywhere after this task:

  src/mangaDexApi.ts
  src/useMangaDexSearch.ts
  src/MangaDexSearch.tsx
  src/MangaDexDetail.tsx

═══ TASK 3 of 9: Create src/reader/useFolderScanner.ts (MODIFIED) ═══

Copy src/useFolderScanner.ts to src/reader/useFolderScanner.ts.
Make exactly ONE change: find this exact string:

  "Folder scanning is not supported in this browser. Use Chrome or Edge."

Replace it with:

  "Folder scanning is not supported in this environment."

Fix imports: any from "./types" → from "../types".
Do not change anything else.

═══ TASK 4 of 9: Create src/reader/useCbzLoader.ts (MODIFIED) ═══

Copy src/useCbzLoader.ts to src/reader/useCbzLoader.ts.
Make these changes:

1. Remove the entire loadUrls function and its export. This means:
   - Delete the full function body for loadUrls (it was MangaDex-only)
   - Remove "loadUrls" from the return object at the bottom of the hook
   - Remove "loadUrls" from the return type if it has one

2. Fix imports: any from "./types" → from "../types"

Do not change anything else.

═══ TASK 5 of 9: Create src/reader/useReadingHistory.ts (MODIFIED) ═══

This file was already copied in Task 1. Now open
src/reader/useReadingHistory.ts and find the function that loads history
from localStorage (it uses JSON.parse). Replace its return logic with a
filter that drops legacy mangadex entries:

Find the try block that parses localStorage and returns the parsed array.
Add this filter so the return becomes:

  return parsed.filter((e) => e.source === "file" || e.source === "folder");

If that filter is already present, skip this task.
Fix imports: any from "./types" → from "../types".

═══ TASK 6 of 9: Create src/reader/MangaHome.tsx (MODIFIED) ═══

Copy src/MangaHome.tsx to src/reader/MangaHome.tsx.
Make these exact changes:

CHANGE 1 — Remove onSearch from Props:
  Find the Props interface (or type). Remove the line that defines onSearch.
  Example: remove  onSearch?: (q: string) => void;  or similar.

CHANGE 2 — Remove handleSearchSubmit callback:
  Find and delete the entire handleSearchSubmit function/callback.

CHANGE 3 — Remove searchQuery useState:
  Find and delete the line:  const [searchQuery, setSearchQuery] = useState(...)

CHANGE 4 — Remove the search form JSX block:
  Find and delete the entire <form> block that submits a search query
  (it contains the MangaDex search input and submit button).
  Also remove any "Powered by MangaDex" label near it.

CHANGE 5 — Fix history icon:
  Find this line (approximately):
    const icon = entry.source === "mangadex" ? "🌐" : "📖";
  Replace with:
    const icon = entry.source === "folder" ? "📂" : "📖";

CHANGE 6 — Fix empty state text:
  Find the string:
    "Search MangaDex above, open a folder, or drop a .cbz file anywhere on this page."
  Replace with:
    "Open a folder to scan your library, or drop a .cbz file anywhere on this page."

CHANGE 7 — Fix root div height:
  Find the root div of the component that has className containing "min-h-screen".
  Change "min-h-screen" to "h-full" in that className.

Fix imports:
  from "./types" → from "../types"
  from "./useReadingHistory" → from "./useReadingHistory"  (same folder, no change)
  from "./useFolderScanner" → from "./useFolderScanner"    (same folder, no change)

═══ TASK 7 of 9: Create src/reader/ReaderScreen.tsx (from MangaReaderScreen.tsx, HEAVILY MODIFIED) ═══

Copy src/MangaReaderScreen.tsx to src/reader/ReaderScreen.tsx.
Make these changes:

CHANGE 1 — Remove MangaDex imports:
  Delete these import lines (exact names may vary slightly, remove any line
  importing from these deleted files):
    import { useMangaDexSearch } from ...
    import { getChapterPages, getChapterLabel, getMangaTitle } from ...mangaDexApi...
    import MangaDexSearch from ...
    import MangaDexDetail from ...
    import type { MangaDexManga, MangaDexChapter } from ...

CHANGE 2 — Remove MangaDex state:
  Delete these useState/hook lines:
    const { state: searchState, ... } = useMangaDexSearch();
    const [activeManga, setActiveManga] = useState<MangaDexManga | null>(null);
    const [onlineError, setOnlineError] = useState<string | null>(null);
  Also remove any state setters for those (setActiveManga, setOnlineError, etc.)

CHANGE 3 — Change View type:
  Find:  type View = "home" | "library" | "search" | "detail" | "reader";
  Replace with:  type View = "home" | "library" | "reader";

CHANGE 4 — Change activeSource type:
  Find any type or variable for activeSource that includes "mangadex".
  Remove "mangadex" from it. Result: "file" | "folder" only.

CHANGE 5 — Remove onSearch from MangaHome usage:
  Find where MangaHome is rendered. Remove the onSearch prop from it.

CHANGE 6 — Remove case "search" and case "detail" from view router:
  Find the view-switching logic (if/else or switch). Remove the branches
  for "search" and "detail" views. Only "home", "library", "reader" remain.

CHANGE 7 — Remove loadUrls from useCbzLoader destructuring:
  Find:  const { ..., loadUrls, ... } = useCbzLoader()
  Remove loadUrls from that destructuring.
  Also delete any code that calls loadUrls(...).

CHANGE 8 — Fix root div:
  Find the root div className. If it has "min-h-screen", change to "h-full".
  Add "flex flex-col" if not already present.

Fix ALL imports to point to correct paths:
  from "./MangaHome"        → from "./MangaHome"           (same folder ✓)
  from "./MangaLibrary"     → from "./MangaLibrary"        (same folder ✓)
  from "./MangaReader"      → from "./MangaReader"         (same folder ✓)
  from "./MangaControls"    → from "./MangaControls"       (same folder ✓)
  from "./MangaToolbar"     → from "./MangaToolbar"        (same folder ✓)
  from "./MangaUploader"    → from "./MangaUploader"       (same folder ✓)
  from "./useCbzLoader"     → from "./useCbzLoader"        (same folder ✓)
  from "./useFolderScanner" → from "./useFolderScanner"    (same folder ✓)
  from "./useKeyboardNav"   → from "./useKeyboardNav"      (same folder ✓)
  from "./useReaderState"   → from "./useReaderState"      (same folder ✓)
  from "./useReadingHistory"→ from "./useReadingHistory"   (same folder ✓)
  from "../types"           → from "../types"              (up one level ✓)
  from "./types"            → from "../types"

═══ TASK 8 of 9: Delete original src/ files that were moved ═══

Delete these files from src/ (the originals — the copies in src/reader/ stay):

  src/MangaControls.tsx
  src/MangaHome.tsx
  src/MangaLibrary.tsx
  src/MangaReader.tsx
  src/MangaReaderScreen.tsx
  src/MangaToolbar.tsx
  src/MangaUploader.tsx
  src/useCbzLoader.ts
  src/useFolderScanner.ts
  src/useKeyboardNav.ts
  src/useReaderState.ts
  src/useReadingHistory.ts

═══ TASK 9 of 9: Verify src/ root only has expected files ═══

After all tasks, src/ root should contain ONLY:
  types.ts, env.d.ts, index.html, index.css, main.tsx

═══ VERIFICATION — Run these shell commands and show me the output ═══

1.  ls manga/src/
2.  ls manga/src/reader/
3.  grep -r "mangaDexApi\|useMangaDexSearch\|MangaDexSearch\|MangaDexDetail\|MangaDexManga\|MangaDexChapter\|MangaDexPageData" manga/src/ || echo "CLEAN"
4.  grep -r "source.*mangadex\|mangadex.*source" manga/src/ || echo "CLEAN"
5.  grep -n "type View" manga/src/reader/ReaderScreen.tsx
6.  grep -n "onSearch" manga/src/reader/ReaderScreen.tsx || echo "CLEAN"
7.  grep -n "loadUrls" manga/src/reader/ReaderScreen.tsx || echo "CLEAN"
8.  grep -n "loadUrls" manga/src/reader/useCbzLoader.ts || echo "CLEAN"
9.  grep -n "min-h-screen" manga/src/reader/MangaHome.tsx || echo "CLEAN"
10. grep -n "source.*folder\|folder.*source" manga/src/reader/MangaHome.tsx
11. grep -n "Folder scanning" manga/src/reader/useFolderScanner.ts
12. grep -n "e.source.*file\|e.source.*folder" manga/src/reader/useReadingHistory.ts

Stop here. Do not proceed to Phase 5. Report the output of all 12 commands.