{
  "name": "app-launcher",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Launcher app (Pixi default, DOM fallback). OPSI 3 structure.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5000",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pixi/unsafe-eval": "^7.4.0",
    "pixi.js": "^7.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
